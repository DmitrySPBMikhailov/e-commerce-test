import SectionTitle from "./SectionTitle";
import ProductsGrid from "./ProductsGrid";

function FeaturedProducts() {
  return (
    <section className="pt-24">
      <SectionTitle text="featured produts" />
      <ProductsGrid />
    </section>
  );
}
export default FeaturedProducts;
